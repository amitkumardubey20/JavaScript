const accountId=122;
let accountEmail="amitkrdubeyyahoo.com";
var accountPassword="1234"

//accountId=123;

// const types of variables can't be changed once assigned

console.log(accountId); 
accountCity="Mahoba"   

let accountState

console.table([accountId, accountEmail, accountPassword, accountCity,accountState])

// console.table ---> displays all the values in a table format
// var is global scope
// let is local scope
// if a variable is not assigned then its output would be undefined.

// You cannot re-declare a variable declared with let or const.
// but with var we can re-declare the same variable.